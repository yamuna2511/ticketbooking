apiVersion: v1
kind: Service
metadata:
  name: ticketbooking
  namespace: development
spec:
  selector:
    app: ticketbooking
  ports:
    - protocol: TCP
      port: 8080
      targetPort: 8080
  type: ClusterIP